# ⚠️ DEPRECATED

This directory (`services/backend_legacy`) is officially deprecated.

All active backend development, including local API mocking and lightweight server integration, has moved to [`services/backend_lite`](../backend_lite).
This code is kept purely for historical reference and backward compatibility checks. It is no longer maintained.
