# ⚠️ DEPRECATED

This directory (`apps/web-legacy`) is officially deprecated.

All active frontend development has moved to [`apps/web-new`](../web-new).
This code is kept purely for historical reference and backward compatibility checks. It is no longer maintained.
